# fastmvc_identity

Identity provider abstractions (`IIdentityProvider`, `IdentityUserProfile`) and `build_identity_providers()` for FastMVC. Uses `IdentityProvidersConfiguration` from `fastmvc_core`. Install optional dependency `fastmvc_identity[oauth]` for httpx-based OAuth2 flows.

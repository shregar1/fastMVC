# fastmvc_payments

Payments configuration DTOs, `PaymentsConfiguration` loader, and base abstractions (`CheckoutSession`, `IPaymentGateway`) for FastMVC apps.

Depends on `fastmvc_core` for config resolution. Use `config/payments/config.json` (or `FASTMVC_CONFIG_BASE`) for payment provider settings.
